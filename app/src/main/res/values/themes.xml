<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DiyouGames" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/text_primary</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
    </style>

    <!-- Full-screen immersive for game screens -->
    <style name="Theme.DiyouGames.Fullscreen" parent="Theme.DiyouGames">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Pixel button style -->
    <style name="Widget.DiyouGames.PixelButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/btn_primary</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="cornerRadius">4dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/btn_border</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <!-- Combat button style (larger, for in-game controls) -->
    <style name="Widget.DiyouGames.CombatButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/btn_primary</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">12sp</item>
        <item name="cornerRadius">8dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/btn_border</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:padding">4dp</item>
        <item name="insetTop">0dp</item>
        <item name="insetBottom">0dp</item>
    </style>

    <!-- Attack button (red) -->
    <style name="Widget.DiyouGames.CombatButton.Attack">
        <item name="backgroundTint">@color/btn_attack</item>
    </style>

    <!-- Special button (gold) -->
    <style name="Widget.DiyouGames.CombatButton.Special">
        <item name="backgroundTint">@color/btn_special</item>
    </style>

    <!-- Block button (blue) -->
    <style name="Widget.DiyouGames.CombatButton.Block">
        <item name="backgroundTint">@color/btn_block</item>
    </style>

    <!-- Jump button (green) -->
    <style name="Widget.DiyouGames.CombatButton.Jump">
        <item name="backgroundTint">@color/btn_jump</item>
    </style>

</resources>
